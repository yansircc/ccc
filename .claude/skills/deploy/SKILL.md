---
name: deploy
description: Build and deploy the ccc binary to ~/.local/bin. Use when the user says "deploy", "build and replace", or wants to install the latest ccc build.
---

Run `scripts/deploy.sh` from the project root (`/Users/yansir/code/52/ccc`).

If the build fails, diagnose the Go compilation error before retrying.
